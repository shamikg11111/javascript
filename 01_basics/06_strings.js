// an old fashioned way of concatenating strings
const name="shamik"
const rep=10

console.log(name + rep )

//a better readable way - using backticks (string interpolation)
console.log(`hello my name is ${name} and ive done ${rep} reps`)
